{% extends "landing/layout.jinja" %}
{% set page_id = 'services_pg' %}

{% block page_sections %}
  <!-- Features Section -->
  <section class="services light-background">  
    <div class="container">
        <div class="row gy-4 mt-0">
        {% for item in items %}
            <!-- feature Item -->
            <div class="col-lg-4 col-md-6 d-flex mt-1">
              <div class="card">
                <div class="card-body">
                  <h4 class="card-title">{{ item.title }}</h4>
                  <div class="card-text">{{ item.description }}</div>
                  <div class="d-flex justify-content-start my-0">
                    <a href="#" class="btn-read-more stretched-link ms-0">
                        {{ item.action }}<i class="bi bi-three-dots pt-2"></i>                  
                    </a>
                  </div>
                </div>
              </div>
            </div> <!-- End feature Item -->
        {% endfor %}
        </div>
    </div>  
  </section><!-- End Features Section -->
  
  <!-- Features Section -->
  <section class="services">  
    <div class="container">
        <div class="row gy-4 mt-0">
        {% for item in items %}
            <!-- feature Item -->
            <div class="col-lg-4 col-md-6 services-item d-flex mt-1">
              <div class="card">
                <div class="card-body">
                  <h4 class="card-title">{{ item.title }}</h4>
                  <div class="card-text">{{ item.description }}</div>
                  <div class="d-flex justify-content-start">
                    <a href="#" class="btn-read-more stretched-link ms-0">
                      {{ item.action }}<i class="bi bi-three-dots pt-2"></i>
                    </a>
                  </div>
                </div>
              </div>
            </div> <!-- End feature Item -->
        {% endfor %}
        </div>
    </div>  
  </section><!-- End Features Section -->
{% endblock %}